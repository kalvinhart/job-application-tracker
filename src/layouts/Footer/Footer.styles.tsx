import styled from "styled-components";

export const StyledFooter = styled.footer`
  min-height: 300px;
  padding: 20px;
  padding-left: calc(var(--side-drawer-width) + 20px);
  background-color: var(--clr-mid);
`;
