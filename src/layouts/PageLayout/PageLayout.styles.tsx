import { styled } from "styled-components";

export const ContentWrapper = styled.div`
  padding-top: var(--header-height);
  padding-left: var(--side-drawer-width);
  display: flex;
  width: 100%;
`;
